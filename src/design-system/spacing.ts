/**
 * Normalized to a 4pt grid from the mockups' approximate values
 * (which ranged from 4-26px across screens).
 */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
} as const;
